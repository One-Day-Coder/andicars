# Components

Los componentes de dominio fueron movidos a `modules/`.

Usar esta carpeta solo si en el futuro aparece un componente verdaderamente global que no pertenezca a ningun modulo. Para stock, CRM, gastos, ventas, reportes, configuracion, usuarios o web publica, crear o editar archivos dentro de `modules/`.
